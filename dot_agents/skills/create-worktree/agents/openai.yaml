interface:
  display_name: "Create Worktree"
  short_description: "브랜치 또는 PR용 Git worktree를 Orca로 생성·재개하고 계보 연결 (tmux 세션은 명시할 때만)"
  default_prompt: "Use $create-worktree to create or resume an Orca worktree for https://github.com/owner/repo/pull/1247 and verify its lineage."
