# Spec 개정 노트 (재설계 실행)

## 라운드 2 개정

이 실행 라운드 1 리뷰(SPEC-016~SPEC-022)를 반영한 개정이다. 라운드 1 본문은 이전 실행 PASS 본과 바이트 동일했고, 이 개정으로 처음 달라진다. 아래 표는 revision_check 파급표의 요구사항 전부를 담는다.

| 요구사항 | 해소 finding | 함께 바뀐 항목 | 상호작용 판정 | 치환 근거 |
|---|---|---|---|---|
| R2.1 | 해당 finding 없음(행 이동) | AC-5(일치), AC-10(일치), AC-12(일치), AC-13(일치), AC-27(일치) | 본문 요구사항 불변. 헤더·문맥 문단 추가로 행이 이동했거나 매핑 AC 문장이 바뀌었다. 판정 수단은 그대로다. | 치환 없음 |
| R3.1 | SPEC-022 | AC-7(일치), AC-8(일치) | 접근 실패 네 outcome 에 reason 값을 붙이고 '실행되어 실패' 로 한정. AC-7·AC-8 판정 불변. | 치환 없음(reason 추가) |
| R3.5 | SPEC-017, SPEC-022 | AC-13(일치), AC-35(일치) | 요청 발행 순서 ①~⑧ 을 요구사항으로 고정하고 예산 소진 저장소의 outcome(checked+reason / budget-exhausted)을 정의. AC-13 이 소진 지점·outcome 을, AC-35 가 사례별 코드를 판정. | '남은 저장소·패턴을 failed_scopes 에' 한 문장을 순서·outcome 규칙으로 치환 — 소진 지점의 결정적 재현을 위함 |
| R5.1 | SPEC-016 | AC-18(일치), AC-49(일치) | recheck 가변 아홉 필드와 불변 스무 필드를 열거해 R7.1 과 정렬. AC-18 판정 불변, AC-49 불변. | 'recheck 는 (b) 를 바꾸지 않는다' 를 가변/불변 열거로 치환 — R7.1 과 모순됐기 때문 |
| R5.3 | SPEC-018, SPEC-019 | AC-20(일치), AC-30(일치) | 스냅숏 evidence 에 duplicate_verdict·observed_head_sha 추가, 승계 필드 넷. AC-20·AC-30 이 판정. | 치환 없음(필드 추가) |
| R6.3 | SPEC-018 | AC-24(일치) | 여덟째 모순(confirmed + 빈 judgment) 추가, duplicate_verdict 보존 명시. AC-24 가 여덟 모순과 보존을 판정. | 치환 없음 |
| R7.1 | SPEC-019 | AC-30(일치) | recheck 스냅숏의 verified_at(재관측 시각)·verified_base_sha(불변)·observed_head_sha·null 후보 규칙을 분기별로 명시. AC-30 이 판정. | '같으면 verified_at 을 갱신한' 을 모든 분기의 값 규칙으로 치환 |
| R7.3 | 해당 finding 없음(행 이동) | AC-30(일치), AC-32(일치) | 본문 요구사항 불변. 헤더·문맥 문단 추가로 행이 이동했거나 매핑 AC 문장이 바뀌었다. 판정 수단은 그대로다. | 치환 없음 |
| R8.1 | SPEC-022 | AC-13(일치), AC-33(일치) | repositories[] 완전성·항목 다섯 키·outcome 여섯 값·reason 값을 명시. AC-13·AC-33 이 판정. | outcome 다섯 값을 여섯 값(budget-exhausted 추가)으로 치환 — ① 미시도 저장소를 표현할 값이 없었기 때문(D12) |
| R8.2 | SPEC-022 | AC-13(일치), AC-35(일치) | 종료 코드 단일 우선순위(전부 접근 실패만 4, 소진은 항상 3) 명시. AC-13·AC-35 가 사례별로 판정. | '하나라도 … 3' 과 '4 = 출력 없는 실패' 병렬 문장을 우선순위 규칙으로 치환 |
| R10.2 | 해당 finding 없음(행 이동) | AC-34(일치), AC-39(일치), AC-30(일치), AC-23(일치) | 본문 요구사항 불변. 헤더·문맥 문단 추가로 행이 이동했거나 매핑 AC 문장이 바뀌었다. 판정 수단은 그대로다. | 치환 없음 |
| R10.4 | SPEC-020 | AC-45(일치), AC-46(일치) | 본문 불변. AC-46/CMD-5 허용 접두어를 스킬 디렉터리·이 실행 문서·기준선 dirty 경로 셋으로 재정의. | 치환 없음(AC 문장 변경) |
| R10.5 | SPEC-020 | AC-47(일치) | 본문 불변. AC-47 의 report.md 를 이 실행 디렉터리로 특정. | 치환 없음 |
| R11.1 | SPEC-020 | AC-47(일치), AC-51(일치), AC-52(일치) | 문서 경로를 이 실행 디렉터리로, 이전 디렉터리는 변경 금지 + preserved-run.sha256 판정. AC-47·AC-51·AC-52 가 판정. | 이전 실행 디렉터리를 이 실행 디렉터리로 치환 — 보존 대상 덮어쓰기와 공허한 통과를 막기 위함(D13) |
| R11.2 | 해당 finding 없음(행 이동) | AC-41(일치) | 본문 요구사항 불변. 헤더·문맥 문단 추가로 행이 이동했거나 매핑 AC 문장이 바뀌었다. 판정 수단은 그대로다. | 치환 없음 |

SPEC-021 은 머리말(Task ID·Lineage) 수정으로, SPEC-020 은 R11.1·AC-46·AC-47·AC-51·AC-52(신설)·CMD-5·CMD-7·CMD-8(신설)·D13 으로 해소했다. 리뷰어가 미확인으로 남긴 두 조건은 `.claude/quality-state/<이 실행 ID>/evidence/orchestrator-facts-r2.txt` 에 원문 출력으로 첨부했다(두 문서 디렉터리 모두 미추적, 형제 스위트 30·108 OK, -k 무매칭 5, quick_validate.py 존재, PyYAML 6.0.3, HEAD 6d60011).
