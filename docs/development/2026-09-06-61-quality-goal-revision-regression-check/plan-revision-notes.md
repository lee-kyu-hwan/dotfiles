# Plan 개정 노트

이 문서는 작성자의 주장이다. 근거가 아니라 검증 대상이며, 각 항목이 실제로 반영됐는지는 `plan.md` 본문에서 확인해야 한다.

## 라운드 2 개정

공식 리뷰 라운드 1(score 83, REVISE, blocker PLAN-01 High, Medium PLAN-02·03, Low PLAN-04·05) 반영. Plan 은 2 라운드 한도라 이 라운드가 마지막이다. 개정 전 230행 → 개정 후 286행 / 태스크 8 / 추적표 54행.

base 는 라운드 1 기록 시점 사본 `plan-r1-snapshot.md`. 프로토타입 diff 로 건드린 요구사항 23건(추적표 행 번호 이동과 소유 태스크 변경으로 넓게 잡힘), 대칭 칸 270 개 중 빈 칸 0. 아래 `함께 바뀐 항목` 열은 손으로 쓰지 않았다 — 프로토타입 파급표(`evidence/proto-self-plan-r2.md`)의 AC 목록을 그대로 옮기고 AC 마다 판정을 붙였다(Spec D12).

### PLAN-01 종류의 전수 대조 (사용자 지침)

PLAN-01 은 "AC 의 기대 결과가 다른 태스크의 산출물에 의존하는데 의존 간선이 없다" 는 종류다. 그 한 건만 고치지 않고 54 AC 전부에 대해 기대 결과·통과 확인이 언급하는 산출물의 생산 태스크를 뽑아 소유 태스크와 같거나 선행인지 대조했다(`evidence/plan-dependency-check-r2.md`). 첫 실행에서 8 건이 표시됐고 그중 셋이 실제 결손이었다.

| AC | 결손 | 해소(문언 변경 최소) |
|---|---|---|
| AC-49 (PLAN-01) | 후반이 T5 의 `record-review` 거부에 의존, 간선 없음 | T5→T4 간선 + T4 착수 조건. 소유는 T4 유지 |
| AC-2 | T1 의 스키마로 출력을 검증하는데 T1→T2 간선 없음 | T1→T2 간선 + T2 착수 조건 |
| AC-3 | 파급표 출력(`ripple`)은 T4 산출물인데 T2 소유 | 소유를 T4 로 이동 |
| AC-53 | 후반의 base 선택이 점검 스크립트에 의존 | `base_digest` 산출을 T2 골격으로 명시하고 T2→T5 간선. 소유는 T5 유지(Spec AC-47 의 `test_quality_state.py` 배치를 지킴) |

나머지 다섯(AC-15·32·34·38·48·52 중 남은 것)은 키워드 오탐이다 — 픽스처 문자열 안의 파일명(AC-15·52), T5 가 스스로 만드는 legacy 픽스처(AC-32), `record-review` 자신의 `--state` 인자(AC-34), 스키마 정의 안의 `ripple` 키(AC-38), T2 파서가 세는 `T` 정의(AC-48). 오탐 판정 근거는 각 AC 문언과 소유 태스크 구현 항목의 대조다.

### T4·T5 참조 위치 전수 (grep 생성)

`grep -n '\bT4\b\|\bT5\b' plan.md` 결과 중 추적표 행(각 AC 행)을 제외한 위치와 갱신 내용이다. 추적표 행은 소유 이동(AC-3: T2→T4)만 바뀌었다.

| 위치 | 내용 | 갱신 |
|---|---|---|
| § Task dependencies 도식 | T1→T2, T2→T3, T2→T5, T3·T5→T4 | 간선 셋 추가·정리 |
| § Task dependencies 수행 순서 문장 | T1 → T2 → T3 → T5 → T4 → T6 → T7 → T8, 병행 조건 | 재작성 + 대조 방법 명시 |
| T2 착수 조건 | T1 완료 | 신설 |
| T2 구현 (4) | `--state` 골격 범위와 T4 로 넘기는 것 | PLAN-02 배타 기술 |
| T4 착수 조건 | T3·T5 완료, AC-49·AC-3 근거 | 신설 |
| T4 구현 (1) | 골격 위의 완성 범위 | PLAN-02 배타 기술 |
| T5 착수 조건 | T1·T2 완료, AC-53 근거 | 신설 |
| T8 구현 | AC-54 스캐너가 T5 규칙을 씀 | 변경 없음 |

### 두 칸 대조와 대칭

`evidence/plan-two-cell-check-r2.md`: 54 AC 전부 "실패 확인에 테스트 작성 단계 / 통과 확인에 등장 / Spec AC-47 파일 배치" 세 칸 빈 칸 0. `evidence/proto-self-plan-r2.md`: 270 칸 빈 칸 0.

| 요구사항 | 해소 finding | 함께 바뀐 항목 | 상호작용 판정 | 치환 근거 |
|---|---|---|---|---|
| R1.1 | PLAN-02 | 생성 목록: AC-1(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T2 본문의 실패·통과 확인에 등장), AC-18(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장), AC-23(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장); 태스크 T2, T4 | T2 의 `--state` 골격(경로 해소·`base_digest`·배타)과 T4 의 완성(digest 대조·의미)을 배타적으로 나눴다. AC-1·AC-2 는 골격만으로 성립한다 | 치환 없음 |
| R1.2 | PLAN-02 | 생성 목록: AC-2(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T2 본문의 실패·통과 확인에 등장), AC-18(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장), AC-26(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장), AC-38(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T1 본문의 실패·통과 확인에 등장), AC-49(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장); 태스크 T1, T2, T4 | AC-2 의 스키마 검증은 T1 산출물이라 T1→T2 간선을 추가했다. `round`·digest 산출은 T2 골격 | 치환 없음 |
| R1.3 | PLAN-01 | 생성 목록: AC-3(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장, 소유 T2→T4); 태스크 T4 | AC-3 의 파급표 출력은 T4 산출물이라 소유를 T4 로 옮겼다(문언 변경 최소). T2 의 stdout 표 골격은 그대로 | 치환 없음 |
| R1.4 | 자체 개정(생성 목록 재작성·간선 추가의 파급) | 생성 목록: AC-4(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T2 본문의 실패·통과 확인에 등장), AC-9(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T2 본문의 실패·통과 확인에 등장), AC-16(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T3 본문의 실패·통과 확인에 등장), AC-37(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T6 본문의 실패·통과 확인에 등장), AC-48(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T2 본문의 실패·통과 확인에 등장), AC-50(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T3 본문의 실패·통과 확인에 등장), AC-52(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T3 본문의 실패·통과 확인에 등장); 태스크 T2, T3, T6 | AC 문언·판정 수단은 그대로이고 태스크 소유·간선만 재검증됐다. 두 칸 대조·의존 대조 모두 빈 칸 0 | 치환 없음 |
| R1.5 | 자체 개정(생성 목록 재작성·간선 추가의 파급) | 생성 목록: AC-5(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T2 본문의 실패·통과 확인에 등장), AC-42(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T7 본문의 실패·통과 확인에 등장), AC-50(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T3 본문의 실패·통과 확인에 등장); 태스크 T2, T3, T7 | AC 문언·판정 수단은 그대로이고 태스크 소유·간선만 재검증됐다. 두 칸 대조·의존 대조 모두 빈 칸 0 | 치환 없음 |
| R1.6 | 자체 개정(생성 목록 재작성·간선 추가의 파급) | 생성 목록: AC-6(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T2 본문의 실패·통과 확인에 등장); 태스크 T2 | AC 문언·판정 수단은 그대로이고 태스크 소유·간선만 재검증됐다. 두 칸 대조·의존 대조 모두 빈 칸 0 | 치환 없음 |
| R2.1 | 자체 개정(생성 목록 재작성·간선 추가의 파급) | 생성 목록: AC-7(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T2 본문의 실패·통과 확인에 등장); 태스크 T2 | AC 문언·판정 수단은 그대로이고 태스크 소유·간선만 재검증됐다. 두 칸 대조·의존 대조 모두 빈 칸 0 | 치환 없음 |
| R2.2 | 자체 개정(생성 목록 재작성·간선 추가의 파급) | 생성 목록: AC-8(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T2 본문의 실패·통과 확인에 등장); 태스크 T2 | AC 문언·판정 수단은 그대로이고 태스크 소유·간선만 재검증됐다. 두 칸 대조·의존 대조 모두 빈 칸 0 | 치환 없음 |
| R2.3 | 자체 개정(생성 목록 재작성·간선 추가의 파급) | 생성 목록: AC-9(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T2 본문의 실패·통과 확인에 등장), AC-10(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T2 본문의 실패·통과 확인에 등장); 태스크 T2 | AC 문언·판정 수단은 그대로이고 태스크 소유·간선만 재검증됐다. 두 칸 대조·의존 대조 모두 빈 칸 0 | 치환 없음 |
| R2.4 | 자체 개정(생성 목록 재작성·간선 추가의 파급) | 생성 목록: AC-11(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T2 본문의 실패·통과 확인에 등장); 태스크 T2 | AC 문언·판정 수단은 그대로이고 태스크 소유·간선만 재검증됐다. 두 칸 대조·의존 대조 모두 빈 칸 0 | 치환 없음 |
| R2.5 | 자체 개정(생성 목록 재작성·간선 추가의 파급) | 생성 목록: AC-12(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T2 본문의 실패·통과 확인에 등장); 태스크 T2 | AC 문언·판정 수단은 그대로이고 태스크 소유·간선만 재검증됐다. 두 칸 대조·의존 대조 모두 빈 칸 0 | 치환 없음 |
| R4.1 | PLAN-01 | 생성 목록: AC-1(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T2 본문의 실패·통과 확인에 등장), AC-18(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장), AC-19(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장), AC-49(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장), AC-53(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T5 본문의 실패·통과 확인에 등장); 태스크 T2, T4, T5 | AC-49 후반이 T5 의 `record-review` 거부를 쓰므로 T5→T4 간선과 T4 착수 조건을 넣었다. AC-53 은 T5 에 두고 T2→T5 간선으로 base 선택 의존을 해소했다 | 치환 없음 |
| R4.2 | 자체 개정(생성 목록 재작성·간선 추가의 파급) | 생성 목록: AC-20(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장), AC-21(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장); 태스크 T4 | AC 문언·판정 수단은 그대로이고 태스크 소유·간선만 재검증됐다. 두 칸 대조·의존 대조 모두 빈 칸 0 | 치환 없음 |
| R4.3 | 자체 개정(생성 목록 재작성·간선 추가의 파급) | 생성 목록: AC-22(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장); 태스크 T4 | AC 문언·판정 수단은 그대로이고 태스크 소유·간선만 재검증됐다. 두 칸 대조·의존 대조 모두 빈 칸 0 | 치환 없음 |
| R5.1 | 자체 개정(생성 목록 재작성·간선 추가의 파급) | 생성 목록: AC-23(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장); 태스크 T4 | AC 문언·판정 수단은 그대로이고 태스크 소유·간선만 재검증됐다. 두 칸 대조·의존 대조 모두 빈 칸 0 | 치환 없음 |
| R5.2 | 자체 개정(생성 목록 재작성·간선 추가의 파급) | 생성 목록: AC-24(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장), AC-25(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장), AC-37(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T6 본문의 실패·통과 확인에 등장); 태스크 T4, T6 | AC 문언·판정 수단은 그대로이고 태스크 소유·간선만 재검증됐다. 두 칸 대조·의존 대조 모두 빈 칸 0 | 치환 없음 |
| R5.3 | 자체 개정(생성 목록 재작성·간선 추가의 파급) | 생성 목록: AC-23(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장), AC-25(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장), AC-26(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T4 본문의 실패·통과 확인에 등장); 태스크 T4 | AC 문언·판정 수단은 그대로이고 태스크 소유·간선만 재검증됐다. 두 칸 대조·의존 대조 모두 빈 칸 0 | 치환 없음 |
| R6.1 | 자체 개정(생성 목록 재작성·간선 추가의 파급) | 생성 목록: AC-27(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T5 본문의 실패·통과 확인에 등장), AC-34(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T5 본문의 실패·통과 확인에 등장), AC-53(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T5 본문의 실패·통과 확인에 등장); 태스크 T5 | AC 문언·판정 수단은 그대로이고 태스크 소유·간선만 재검증됐다. 두 칸 대조·의존 대조 모두 빈 칸 0 | 치환 없음 |
| R6.2 | 자체 개정(생성 목록 재작성·간선 추가의 파급) | 생성 목록: AC-28(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T5 본문의 실패·통과 확인에 등장), AC-29(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T5 본문의 실패·통과 확인에 등장), AC-34(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T5 본문의 실패·통과 확인에 등장); 태스크 T5 | AC 문언·판정 수단은 그대로이고 태스크 소유·간선만 재검증됐다. 두 칸 대조·의존 대조 모두 빈 칸 0 | 치환 없음 |
| R6.3 | 자체 개정(생성 목록 재작성·간선 추가의 파급) | 생성 목록: AC-30(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T5 본문의 실패·통과 확인에 등장), AC-31(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T5 본문의 실패·통과 확인에 등장), AC-34(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T5 본문의 실패·통과 확인에 등장); 태스크 T5 | AC 문언·판정 수단은 그대로이고 태스크 소유·간선만 재검증됐다. 두 칸 대조·의존 대조 모두 빈 칸 0 | 치환 없음 |
| R6.4 | 자체 개정(생성 목록 재작성·간선 추가의 파급) | 생성 목록: AC-32(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T5 본문의 실패·통과 확인에 등장), AC-33(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T5 본문의 실패·통과 확인에 등장); 태스크 T5 | AC 문언·판정 수단은 그대로이고 태스크 소유·간선만 재검증됐다. 두 칸 대조·의존 대조 모두 빈 칸 0 | 치환 없음 |
| R7.3 | PLAN-03, PLAN-05 | 생성 목록: AC-42(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T7 본문의 실패·통과 확인에 등장), AC-43(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T8 본문의 실패·통과 확인에 등장), AC-46(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T8 본문의 실패·통과 확인에 등장), AC-54(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T8 본문의 실패·통과 확인에 등장); 태스크 T7, T8 | T8 실패 확인을 "테스트 부재의 종료 코드 5" 로 사실대로 바꾸고 구현과 분리했다. CMD-4·CMD-5 전문을 Plan 에 실었다(Spec 과 같은 스크립트) | 치환 없음 |
| R7.5 | PLAN-03, PLAN-04 | 생성 목록: AC-44(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T8 본문의 실패·통과 확인에 등장), AC-45(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T8 본문의 실패·통과 확인에 등장), AC-47(일치 — 추적행 판정 수단이 Spec 토큰과 같고 소유 태스크 T8 본문의 실패·통과 확인에 등장); 태스크 T8 | CMD-6 이 승인 Spec 에서 빈 칸을 보고할 때의 처리(Spec 불변, 스크립트 결함/재승인 판단)를 T8 에 넣었다 | 치환 없음 |

### Codex 점검(제출 전)

Codex(6.4.2) 에 "각 AC 의 통과 확인이 그 소유 태스크 종료 시점에 실제로 성립하는가" 를 초점으로 읽기 전용 점검을 시켰다. 결과 `evidence/codex-plan-symmetry-r2.md` 를 공식 리뷰 컨텍스트에 첨부한다. 결과가 이 개정을 바꾸면 아래에 덧붙인다.

Codex 점검 결과(`evidence/codex-plan-symmetry-r2.md`): 54 AC 전부 "성립", 불성립 2 는 CMD-4·CMD-5 전문이 Spec 과 "같은 스크립트" 라는 문언과 달리 인터프리터가 `python3` → `"$QG_PY"` 로 바뀌었고 CMD-5 의 주석이 빠졌다는 지적이다. 문언을 "같은 검사, 인터프리터만 다름" 으로 고치고 주석을 되살렸다. 검사 논리는 동일하다.

Codex 점검 결과(`evidence/codex-plan-symmetry-r2.md`): 54 AC 전부 "성립", 불성립 2 는 CMD-4·CMD-5 전문이 Spec 과 "같은 스크립트" 라는 문언과 달리 인터프리터가 `python3` → `"$QG_PY"` 로 바뀌었고 CMD-5 의 주석이 빠졌다는 지적이다. 문언을 "같은 검사, 인터프리터만 다름" 으로 고치고 주석을 되살렸다. 검사 논리는 동일하다.
