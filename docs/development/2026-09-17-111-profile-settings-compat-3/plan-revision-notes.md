## 라운드 2 개정

라운드 1 공식 리뷰의 blocker `PLAN-001`~`PLAN-004`와 `PLAN-005`~`PLAN-008`을 해소했다. 오케스트레이터가 Spec의 27개 AC 판정 집합을 전수 대조하는 과정에서 `AC-10`과 `AC-26`이 Spec에서는 `[문서]` 판정인데 Plan이 실행 명령으로 적은 불일치를 추가로 찾아 함께 고쳤다. 태스크 구조와 추적표가 넓게 바뀌어 모든 요구사항이 touched로 잡힌다.

| 요구사항 | 해소 finding | 함께 바뀐 항목 | 상호작용 판정 | 치환 근거 |
|---|---|---|---|---|
| R1.1 | PLAN-007 | AC-1(일치: T4가 수정 지점 2 `:3014` 편집과 `test_claude_official_status_contract` 판정을 함께 소유), AC-2(일치: 판정 집합 불변) | argv 하드닝 축과 그 축이 뒤집는 기존 단언을 같은 태스크에 두어 T6과의 순서 의존을 없애며 다른 해소의 전제를 깨지 않는다. | 라운드 1은 `:3014` 편집을 T6에 두어 AC-1 소유 태스크(T4)가 스스로 통과할 수 없었으므로 T4로 옮겼다. |
| R1.2 | PLAN-001, PLAN-004, PLAN-006, PLAN-007 | AC-3(일치: T5 B 소유 유지), AC-10(일치: Spec대로 `[문서]` 판정으로 정정), AC-12(일치: T5 B 소유 유지), AC-19(일치: `test_portable_cli_contract`를 판정 집합에 추가하고 T5 A가 Claude in-range test-local fake 편집 소유), AC-20(일치: 기존 두 문서 계약 테스트를 판정 집합에 추가) | CLI 계약 변경을 T1 stage A에 전부 포함하고 helper 편집을 G12의 정확한 두 줄로 고정해 실행 기반 분류와 최종 편집 경계 검사가 같은 대상을 본다. | 라운드 1 T1이 CLI 축 일부만 적용하고 helper 선택 인자를 허용했던 서술을 production 전체 적용과 두 줄 한정으로 치환했다. |
| R1.3 | PLAN-001, PLAN-002, PLAN-003 | AC-4(일치: T3 소유 유지), AC-5(일치: `test_settings_compat_launch_default_verifier`를 판정에 추가해 launch 기본 verifier 채움 검증), AC-15(일치: T1 실행 기반 분류와 대조), AC-22(일치: `test_settings_compat_launch_prescan_gate`를 판정에 추가해 R1.3(1) 양성·여섯 음성 확인), AC-23(일치: 판정 집합 불변), AC-24(일치: 판정 집합 불변), AC-25(일치: T2 Red 선행 유지) | R1.3(1) launch skip과 R1.3(4) admitted prescan 제거를 T5 B에 함께 두고 launch `--verifier` 선택화를 T3에 두어 소유 태스크 없는 production 변경을 없애며 status 비치명 술어와 충돌하지 않는다. | 라운드 1은 R1.3(1)과 launch 기본 채움을 File map에만 적고 소유 태스크를 두지 않아 T3·T5에 명시 태스크로 치환했다. |
| R1.4 | 치환 없음(추적표 서식 정렬) | AC-6(일치: T10 판정 집합과 선작성 실패 관측 서술 유지) | live projection 판정 내용과 opt-in 경계를 바꾸지 않아 새 공백을 만들지 않는다. | 자체 개정 |
| R2.1 | PLAN-001, PLAN-002 | AC-4(일치: 정본 verifier 양성 판정 유지), AC-7(일치: T7 판정 유지), AC-18(일치: 교차 회귀가 R1.3(1) 적용 뒤 성립), AC-23(일치: 운영 명령 판정 유지), AC-24(일치: 비정본 차단 판정 유지), AC-25(일치: Red 전사 유지) | profile1 성공 조건이 이제 T3·T5 B의 명시 태스크에서 만들어지므로 T7이 소유 없는 변경에 기대지 않으며 다른 해소를 깨지 않는다. | 자체 개정 |
| R2.2 | PLAN-002 | AC-8(일치: launch skip이 T5 B에서 구현된 뒤 성립), AC-12(일치: role sentinel 판정 유지), AC-18(일치: 교차 회귀 판정 유지), AC-20(일치: 기존 문서 계약 테스트 포함) | role launch 성공 경로의 production 소유가 T5 B로 확정돼 E2E 태스크와의 의존이 명확해지며 새 공백을 만들지 않는다. | 자체 개정 |
| R2.3 | PLAN-007 | AC-9(일치: 판정 집합 불변), AC-10(일치: Spec대로 `[문서]` 판정으로 정정) | profile2 전경 enrollment 금지 전제와 세 단계 순서를 그대로 두고 판정 방식만 Spec과 맞춰 자동 실행 금지 제약을 깨지 않는다. | 라운드 1이 AC-10을 실행 명령으로 적었던 것을 Spec의 `[문서]` 판정으로 치환했다. |
| R3.1 | PLAN-002 | AC-11(일치: T4 sentinel 판정 유지), AC-12(일치: T5 B sentinel 판정 유지), AC-13(일치: T9 격리 판정 유지), AC-18(일치: 교차 회귀가 launch skip 적용 뒤 주입 0개와 동시 성립) | launch skip의 여섯 음성 조건이 prescan 유지를 증명하므로 보안 판정 강도가 약해지지 않는다. | 자체 개정 |
| R3.2 | PLAN-001, PLAN-004, PLAN-005, PLAN-007, PLAN-008 | AC-1(일치: 수정 지점 2 소유를 T4로 정렬), AC-13(일치: 격리 판정 유지), AC-14(일치: T11 편집 경계 검사가 29개 source 동일성 확인), AC-15(일치: T1 stage B 실행으로 분류를 확정하고 CMD-5 포함), AC-16(일치: 기존 세 테스트를 판정 집합에 모두 명시), AC-17(일치: scope 판정 유지), AC-19(일치: portable 테스트 포함), AC-21(일치: fixture 불변 판정 유지), AC-22(일치: launch skip 테스트 포함), AC-26(일치: Spec대로 `[문서]` 판정으로 정정하고 T1이 production 전체를 적용), AC-27(일치: G8 원시 전사·exact 정규식·상위집합·G9 판정으로 내용 검사 구체화) | 분류를 추정에서 stage A/B 실행 결과로 바꾸고 helper 편집을 두 줄로 한정하며 영구 commit 고정 테스트 대신 일회성 검사 스크립트를 써서 #126과 같은 유지보수 함정을 만들지 않는다. | 라운드 1의 추정 기반 `restored_by_helper`, 라벨만 보는 G8, 정의되지 않은 스크래치 경로를 실행 기반 분류, exact 형식 내용 판정, gitignore된 구체 경로로 치환했다. |
| R3.3 | PLAN-008 | AC-10(일치: `[문서]` 판정으로 정정), AC-17(일치: evidence·scratch 경로가 제외 prefix 안에 있음을 명시), AC-20(일치: 기존 문서 계약 테스트 포함), AC-21(일치: T5 A에서 fixture 편집 부재를 추가 확인), AC-23(일치: 판정 유지), AC-24(일치: 판정 유지) | 스크래치 클론을 `.claude/quality-state` 아래에 두어 G7 비교 집합을 넓히지 않고 허용 10개 파일 범위를 유지한다. | 라운드 1의 미정의 `<scratch>`와 `git checkout`을 gitignore 경로와 `clone -b`로 치환했다. |
