## 라운드 2 개정

| 요구사항 | 해소 finding | 함께 바뀐 항목 | 상호작용 판정 | 치환 근거 |
|---|---|---|---|---|
| R1.2 | SPEC-002 | AC-3(일치: role argv에서 mcp-config 0개), AC-10(일치: 운영자 고지), AC-12(일치: 배포 MCP 존재 아래 child 0개), AC-19(일치: 5-option 계약 유지), AC-20(일치: auth와 role 판단 구분) | role MCP 기능 손실 수용은 정본 status 전환과 비치명 skip 술어의 전제를 바꾸지 않고 문서·보안 판정을 함께 닫는다. | 자체 개정 |
| R1.3 | SPEC-001, SPEC-003, SPEC-005 | AC-4(일치: 정본·주입 대비), AC-5(일치: HOME 출처와 비치명 음성 case), AC-15(일치: 네 prescan 지점 유지), AC-22(일치: project 차단 유지), AC-23(일치: 문서화된 정본 명령), AC-24(일치: source·임의 경로 차단), AC-25(일치: 구현 전 Red 전사) | 정본 양성 경로를 추가하면서 비정본 차단과 기존 blocked_verifier 순서를 보존해 MCP 결정이나 네 축 열거에 새 공백을 만들지 않는다. | 자체 개정 |
| R2.1 | SPEC-001, SPEC-005 | AC-4(일치: 동일 위험 settings 대비), AC-7(일치: 정본 status ready), AC-18(일치: status·launch 교차 판정), AC-23(일치: exact 운영 명령 ready), AC-24(일치: 비정본 blocked_policy), AC-25(일치: positive control Red) | 실제 결함 폐쇄점을 정본 운영 명령으로 고정해 role MCP 0개 결정과 verifier identity 경계를 동시에 만족한다. | 자체 개정 |
| R2.2 | SPEC-002 | AC-8(일치: role session 1회), AC-12(일치: MCP child 0개), AC-18(일치: 주입 0개 교차 판정), AC-20(일치: 사용자 가시 결과 고지) | role launch 성공은 MCP server 0개를 포함하는 성공으로 정의되어 profile1 status ready 계약과 충돌하지 않는다. | 자체 개정 |
| R2.3 | SPEC-002 | AC-9(일치: profile2 lifecycle 유지), AC-10(일치: lifecycle block에 role MCP 결과 고지) | profile2의 enrollment 상태·명령·결과는 바꾸지 않고 같은 운영 block에 role MCP 호환성 결과만 추가해 새 lifecycle 공백을 만들지 않는다. | 자체 개정 |
| R3.1 | SPEC-002 | AC-11(일치: auth MCP child 0개), AC-12(일치: role MCP child 0개), AC-13(일치: 격리 fixture 유지), AC-18(일치: 교차 주입 0개) | MCP server 0개 결정을 기존 sentinel 음성·양성 control에 연결해 정본 status 전환의 보안 전제를 강화하고 탐지 공백을 만들지 않는다. | 자체 개정 |
| R3.2 | SPEC-004, 실측 인계(CLI 계약 blast radius) | AC-1(일치: R1.1 exact argv 재정의 유지), AC-13(일치: 실제 worktree 밖 스크래치 격리), AC-14(일치: 최종 29개 기대 불변과 경로 동일성 유지), AC-15(일치: 수정 지점 4개와 실측 8메서드/10케이스 분리), AC-16(일치: 설치본 회귀 유지), AC-17(일치: byte-only digest 범위), AC-19(일치: portable CLI 계약과 helper help 원인 연결), AC-21(일치: fixture bytes를 바꾸지 않고 test-local helper로 원래 기대 복원), AC-22(일치: 기존 prescan 기대 유지), AC-26(일치: 구현 전 세 축 스크래치 재측정 Plan 태스크), AC-27(일치: 재측정 evidence의 전수 메서드·케이스 판정) | 단일 축 실측을 하한선으로 제한하고 세 축 전수 재측정을 선행시켜 기존 SPEC-001~005의 정본 경로·MCP·fixture·문서 전제를 약화하지 않으며, helper 경유 여섯 메서드는 기대값 변경이 아니라 원래 기대 복원으로 구분한다. | 앞선 수정 지점 중심 충분성 근거를 실측 테스트 영향과 구현 전 세 축 전수 evidence로 보강 |
| R3.3 | SPEC-001, SPEC-002, SPEC-004 | AC-10(일치: profile2·MCP 고지), AC-17(일치: scope와 byte digest), AC-20(일치: 설정 격리·MCP 결과), AC-21(일치: fixture bytes 불변), AC-23(일치: profile1 정본 명령 block), AC-24(일치: 비정본 보안 결과 block) | 새 profile1·MCP 문서 block은 기존 assertIn 문서 계약과 fixture bytes 불변을 유지하고 HOME·verifier 술어 규정과 같은 정본 경로를 사용한다. | 자체 개정 |
