# Plan 개정 노트

기준은 `.claude/quality-state/<task-id>/snapshots/plan-r1.md` 이며 그 이후의 모든 변경을 기록한다. 라운드 1 리뷰는 PASS 88 이었고 blocker 는 없었다. 사용자가 승인 게이트에서 advisory finding 6건을 문서에서 해소하도록 선택해 `AWAITING_PLAN_APPROVAL → PLAN_REVIEW` 로 되돌린 뒤 개정했다.

## 라운드 2 개정

| 요구사항 | 해소 finding | 함께 바뀐 항목 | 상호작용 판정 | 치환 근거 |
|---|---|---|---|---|
| R1.1 | PLAN-005, PLAN-006 | AC-1(일치: 미시작 저장소 사례의 fixture 경로를 `budget/unstarted/case.json` 으로 명시), AC-2(일치: 합집합 비교를 cap 전 universe 하나로 고정하고 다섯 시나리오의 fixture 경로와 신설 여부를 열거), AC-3(일치: 종료 코드 우선순위 서술 불변) | 합집합 기준을 cap 전 universe 로 통일한 것은 R1.1 본문과 AC-2 의 표현을 일치시킬 뿐 구획 정의를 바꾸지 않으므로 R1.2 의 금지 조합 판정이나 R1.3 의 입력 검증 전제를 깨지 않는다. fixture 경로 명시는 판정 수단을 좁히지 않는다. | 자체 개정 |
| R1.2 | PLAN-001 | AC-4(일치: T2 를 T3 뒤로 확정하고 fixture 가 shape validator 를 만족해야 함을 명시), AC-5(일치: 두 금지 분기 단정은 그대로 두고 양립 확인 지점만 T2 로 이동) | T2 를 T3 뒤로 두면 T2 의 fixture 가 shape validator 를 통과해야 하지만, 그 조건은 AC-4·AC-5 가 요구하는 `combo in forbidden_combos` 분기 실행을 방해하지 않는다. 정상 형태의 discovery 에 금지 구획만 추가하면 두 조건이 동시에 성립한다. | 자체 개정 |
| R1.3 | PLAN-001 | AC-6(일치: T3 통과 확인에서 정정 전 기존 테스트만 대조하도록 정정), AC-7(일치: 배열 아닌 구획 판정 불변) | T3 의 통과 확인에서 아직 없는 테스트 참조를 제거해 순서 모순을 없앴다. 대신 T2 의 통과 확인이 `CMD-4` 재실행으로 R1.3 회귀를 막으므로 검증 공백이 생기지 않는다. | 자체 개정 |
| R2.1 | PLAN-002 | AC-8(일치: record 측 정책 게이트 단정 불변), AC-9(일치: 확정 실행 순서를 T7 → T5 로 바꿔 recheck 차단 단정이 T5 종료 시점에 달성 가능해짐) | T7 을 T5 앞으로 옮겨도 T7 의 `CMD-9` 는 세 실패 경로만 다루므로 T5 없이 통과하고, T5 의 정책 판정이 T7 의 전이를 소비하는 방향은 단방향이어서 순환이 없다. 전역 제약에 AC-9 단정의 약화·삭제·skip 금지를 명시해 종료 0 을 얻으려는 우회를 닫았다. | 자체 개정 |
| R4.1 | PLAN-003, PLAN-004 | AC-19(일치: 계약 문서 네 문장 단정 불변), AC-21(일치: 고정 상수·문서 동기화 판정 불변), AC-22(일치: 전체 스위트 판정 불변, 이력 불변성 검증만 CMD-16·CMD-17 로 분리) | `CMD-16` 의 범위를 1차 실행 문서 5개로 좁히고 2차 실행 문서 7개용 `CMD-17` 을 신설한 것은 판정 명령 표에 행을 추가할 뿐 AC 판정 수단을 바꾸지 않는다. rollback 트리거와 재검증 문구도 함께 고쳐 서술 불일치를 남기지 않았다. Spec link 문장의 AC 개수 정정은 추적표와 일치시키는 서술 수정이다. | 자체 개정 |
