## 라운드 2 개정

| 요구사항 | 해소 finding | 함께 바뀐 항목 | 상호작용 판정 | 치환 근거 |
|---|---|---|---|---|
| R2.1 | SPEC-009, READY-003 | AC-8(일치: entry별 frame 선후 관계를 유지), AC-9(일치: regular-file mode 복원 계약 유지), AC-20(일치: nested directory를 canonical 진입 지점에서 frame); Architecture, Interfaces/data flow, Failure behavior, Test strategy, D4, strict Migration/High-risk verification | mixed-kind 정렬을 추가해도 entry별 metadata 선행과 chmod 복원 전제는 깨지지 않고 byte stream만 완전히 결정된다. | 종류별 상대 순서를 열어 둔 r1의 “정렬된 child walk”를 repository-relative byte path 단일 lexicographic stream으로 구체화했다. |
| R2.3 | SPEC-009, READY-003 | AC-8(일치: 각 entry 내부 frame 순서 유지), AC-11(일치: mixed-kind exact sequence와 생성 순서 독립성을 직접 판정), AC-20(일치: directory 진입 frame 직후 depth-first child walk 판정); traceability, Architecture, Interfaces/data flow, Failure behavior, Test strategy, D4, strict Migration/High-risk verification | regular directory의 depth-first subtree와 symlink-directory prune가 하나의 sibling ordering에 결합되어 다른 determinism 또는 safety 해소와 모순되지 않는다. | r1의 kind 간 미정 순서를 repository-relative byte path 단일 lexicographic ordering과 enter-directory framing으로 치환했다. |
| R2.4 | SPEC-009, READY-003 | AC-9(일치: file chmod 변경·복원 유지), AC-11(일치: canonical stream 회귀와 기존 fingerprint 탐지 보존), AC-20(일치: nested-directory chmod 복원과 child 포함 유지); Architecture, Interfaces/data flow, Failure behavior, Test strategy, D4, strict Migration/High-risk verification | canonical emission 순서는 permissions 민감도와 복원성을 바꾸지 않고 동일 tree의 유일한 digest 표현을 강화한다. | r1의 결정성 판정을 mixed-kind sibling fixture의 exact canonical sequence까지 확장했다. |
| R3.1 | SPEC-008, READY-002 | AC-12(일치: 새 절 위치·다섯 요소·last-record-only를 독립 고정), AC-14(일치: exact-guarantee content contract가 집중 회귀에 포함), AC-17(일치: QG_BASE와 모든 protected 절 byte identity를 동시 판정); traceability, Architecture, Interfaces/data flow, Security/risk, Test strategy, strict Migration/Rollback/High-risk verification | authoritative 신규 절과 보호 prose의 stricter clarification 관계를 명시하여 exact guarantee와 보존 검사가 함께 만족되며 R1.2의 backward-search 금지와도 일치한다. | protected 절을 수정하는 r1의 암묵적 가능성을 폐기하고 `## Completion integrity`를 `## Independent verification` 직전에 추가하되 QG_BASE와 protected list는 유지하는 방식으로 구체화했다. |
| R4.2 | SPEC-009, READY-003 | AC-8(일치: 모든 kind의 frame 순서), AC-9(일치: regular-file chmod metamorphic test), AC-10(일치: symlink-directory non-dereference/prune와 special 비읽기), AC-11(일치: mixed-kind 단일 ordering exact sequence), AC-14(일치: 집중 회귀 클래스 통과), AC-20(일치: nested directory 진입과 depth-first child stream); traceability, Architecture, Interfaces/data flow, Failure behavior, Test strategy, D4, strict Migration/High-risk verification | 새 exact sequence fixture가 symlink 안전성과 metadata 복원 테스트를 같은 canonical stream에서 검증하므로 기존 coverage를 약화하지 않는다. | r1의 일반적인 “deterministic sorting”을 이름과 kind가 교차하는 sibling의 repository-relative byte-path expected sequence로 치환했다. |
| R4.3 | SPEC-007, SPEC-008, SPEC-010, READY-001, READY-002, READY-004 | AC-15(일치: supported/tested 3.12.14 identity와 full suite), AC-16(일치: 신규 절 추가 뒤 500줄 제한), AC-17(일치: protected sections byte identity), AC-18(일치: 수정 Python source/test syntax), AC-19(일치: assertion body만 갱신하고 test name 보존); traceability, CMD-2/CMD-8, Test strategy, strict Migration/Rollback/High-risk verification | task-local CMD-8은 canonical CMD-7의 unsupported-runtime 계약을 침범하지 않고, 보호 절 유지와 기존 test name 유지가 각각 CMD-4와 CMD-6을 동시에 만족한다. | r1의 “동명 CMD-8을 override하지 않는다”를 runbook이 CMD-7에서 끝난다는 사실과 canonical CMD-7 제외 근거로 교체하고, 문서 배치·version test 파급을 R4.3 판정망에 명시했다. |
| R4.4 | SPEC-007, SPEC-011, READY-001 | AC-21(일치: 기존 exact frontmatter test와 기존 major-bump test가 6.0.0/major 6을 판정); traceability, CMD-2, Test strategy, strict Migration/Rollback | 두 기존 assertion을 in-place 갱신하고 test name을 보존하므로 full suite와 CMD-6을 깨뜨리지 않으며 별도 중복 version test도 만들지 않는다. | r1의 신규 `test_skill_frontmatter_version_is_6_0_0` 계획을 기존 `test_frontmatter_contract`와 `test_skill_version_is_major_bumped`의 authoritative assertion 갱신으로 치환했다. |
