# Spec 개정 노트

## 승계 판정 — 직전 실행 r3 findings (SPEC-11~15)

이 목표는 2026-09-06 실행(`20260906T100138Z-70-quality-goal-spec-단계에-codex-author-fr-7ec207bb`)의 재시도가 아니라 축소된 요구사항의 새 목표다. 그 실행의 마지막 리뷰(`.claude/quality-state/20260906T100138Z-70-quality-goal-spec-단계에-codex-author-fr-7ec207bb/spec-review-r3.json`, 82점 REVISE, blocker `SPEC-11`)가 남긴 다섯 finding 을 하나씩 판정한다. 판정값은 **#76 이관** 또는 **해소** 둘뿐이며, "해결" 로 표기한 것은 없다.

| finding | severity | 요지 | 판정 | 근거 |
|---|---|---|---|---|
| `SPEC-11` | High | 닫힘이 기록 간에 누적되지 않아 같은 공식 라운드의 세 번째 시도부터 게이트가 영구히 열리지 않는 `RETRY`→`HOLD` 고리 | **#76 이관** | 게이트 판정 함수·유효 Critical/High 집합·시도 구간 한도·`AWAITING_READINESS_DECISION` 전이가 전부 Non-goal 12 로 이관됐다. 이 Spec 에는 판정하는 함수가 없으므로 "열리지 않는 게이트" 라는 상태가 성립하지 않는다. 대체 판정 규칙 자체를 정의하지 않았다(§ Architecture '상태 머신에 대한 변경 — 없음', D1, D4). **해결이 아니라 이관이다** — 게이트를 다시 도입할 때 같은 문제를 처음부터 다시 설계해야 한다 |
| `SPEC-12` | Medium | 판정 기록보다 뒤에 있으면서 등록 digest 를 가리키지 않는 `recorded` 기록의 Critical/High 가 유효 집합의 어느 항에도 들어가지 않아 stale High 가 무시됨 | **#76 이관** | 결함의 대상이 '유효 Critical/High 집합' 과 '판정 기록' 선정 규칙이며 둘 다 게이트 논리다. 이 Spec 은 기록을 판정에 쓰지 않으므로(R6.1 마지막 문장) 집합 정의가 없다 |
| `SPEC-13` | Medium | arbitration 조건을 유효 집합 없이 '가장 최근 기록의 Critical/High 0' 만으로 서술한 자리가 남아 R6.10 과 어긋남 | **#76 이관** | `ARBITRATION` 이라는 반환값과 arbitration 경로 자체가 없다. R4.4 는 근거 첨부만 규정하고 어떤 진행 조건도 만들지 않는다 |
| `SPEC-14` | Medium | 실패 기록(`schema_invalid`·`digest_mismatch`·`invocation_failed`)에서 `resolved_finding_ids` 의 값이 정해지지 않아 상태 기록 스키마와 테스트가 서로 다른 형태를 허용 | **해소** | 축소 범위 안이다. R6.6 이 실패 기록의 표현을 "`verdict` 와 `score` 는 `null`, `checklist`·`findings`·`resolved_finding_ids` 는 빈 배열, 나머지 일곱 필드는 채운다" 로 고정했고 AC-55 가 세 실패 `outcome` 각각에서 그 형태를 판정한다 |
| `SPEC-15` | Low | AC 정의 순서가 번호 순과 어긋남(AC-116 다음에 118·119·120, AC-117 이 맨 끝) | **해소** | 이 Spec 의 AC 는 AC-1 부터 마지막 번호까지 빠짐·중복 없이 번호 순으로 정의된다(라운드 2 개정 후 AC-90 이 마지막이다). 직전 산출물을 그대로 잇지 않고 다시 번호를 매겼으므로 순서 어긋남이 승계되지 않았다 |

이관 사유는 규모가 아니라 설계 난도다. `SPEC-03`→`SPEC-07`→`SPEC-11` 은 하나의 연쇄이며, readiness 게이트의 fail-open/fail-closed 규칙을 고칠 때마다 다음 라운드가 그 수정이 만든 새 구멍을 잡았다. 자세한 근거는 `spec.md` 의 § Problem and context 에 있다.

이 절은 라운드 개정 노트가 아니라 승계 기록이다. 라운드 2 이상의 개정 노트는 아래에 `## 라운드 <n> 개정` 절로 이어 붙인다.

## 라운드 2 개정

라운드 1 공식 리뷰(81점 REVISE, blocker `SPEC-16`)의 아홉 finding 을 모두 반영했다. 아래 표의 `함께 바뀐 항목` 열은 `revision-check-spec-r2.json` 의 `ripple[].acceptance_criteria` 생성값을 그대로 옮긴 것이며 손으로 고른 항목이 아니다.

| 요구사항 | 해소 finding | 함께 바뀐 항목 | 상호작용 판정 | 치환 근거 |
|---|---|---|---|---|
| R7.1 | SPEC-16 | AC-60(일치), AC-61(일치), AC-69(일치) | 발급 자리와 참조 자리를 나눈 결과 AC-60 은 발급 자리만, AC-61 은 참조 자리의 두 접두 허용과 발급 자리의 `SPEC-` 거부를 한 묶음으로 판정해 두 AC 의 중복이 사라졌고, AC-69 가 그 구분을 스키마의 네 패턴으로 판정한다. 다른 해소의 전제를 깨지 않는다 — C8·R8.4 가 같은 구분을 각각 절차와 스키마로 표현하므로 세 자리가 한 규칙을 말한다 | 치환. 제출 전 대칭 점검에서 AC-88 이 R7.1 의 접두 규칙을 판정하지 않는 유령 매핑임이 드러나, 네 패턴을 실제로 판정하는 AC-69 로 바꿨다 |
| R2.1 | SPEC-16 | AC-17(일치), AC-24(일치) | AC-24 에 finding 별 ID·severity·`required_resolution` 가시성과 발급/참조 지시를 더하면서 R2.1(문맥 상속 금지)의 매핑 AC 가 함께 바뀌었다. R2.1 본문은 그대로이고, 확장된 AC-24 도 여전히 '파일 경로로 전달되는 리뷰어 산출물' 만 허용하므로 상속 금지의 전제를 깨지 않는다 | 자체 개정 |
| R3.1 | SPEC-16 | AC-25(일치), AC-26(일치), AC-27(일치), AC-28(일치), AC-68(일치), AC-88(일치) | C8 이 `prior_findings` 에 판정을 남기도록 바뀌어 R8.4 의 새 필드와 짝을 이룬다. C1~C7 과 충족 규칙(AC-26·AC-27)은 건드리지 않았으므로 여덟 항목 체크리스트라는 전제가 유지된다 | 자체 개정 |
| R8.4 | SPEC-16 | AC-68(일치), AC-69(일치), AC-88(일치) | 네 패턴 제약이 R7.1 의 구분을 스키마로 드러낸다. `resolved_finding_ids` 를 `judgement == resolved` 집합과 같게 묶은 것이 C8 의 절차와 일치하며, AC-69(빈 배열 허용)의 전제를 깨지 않는다 | 치환 없음 |
| R8.1 | SPEC-16 | AC-63(일치), AC-64(일치), AC-65(일치) | 필수 필드가 열둘에서 열셋으로 늘어 AC-63 의 판정 대상이 함께 바뀌었다. AC-64(`additionalProperties: false`)와 충돌하지 않는다 — 새 필드를 required 에 넣었으므로 추가 필드가 아니다 | 치환 없음 |
| R2.5 | SPEC-16 | AC-22(일치), AC-23(일치), AC-24(일치), AC-61(일치) | 프롬프트가 참조·발급 구분을 명시하게 되어 fresh reviewer 가 `SPEC-` 를 참조로만 쓰도록 유도된다. R2.1 의 문맥 상속 금지와 충돌하지 않는다 — 전달되는 것은 여전히 파일 경로의 리뷰어 산출물이다 | 자체 개정 |
| R4.4 | SPEC-17 | AC-40(일치) | 첨부를 경로 둘로 고정해 `SKILL.md` § Review invocation contract 의 입력 한정과 양립시켰다. R9.5 의 보존 대상 여덟 절을 건드리지 않으므로 AC-80(CMD-4)의 전제가 유지되고, R4.1 의 비권위성도 그대로다 — 첨부는 근거일 뿐 판정을 구속하지 않는다 | 치환 없음 |
| R6.1 | SPEC-16 | AC-45(일치), AC-46(일치), AC-47(일치), AC-48(일치) | 상태 기록은 열두 필드 그대로 두고 `prior_findings` 를 결과 파일에만 남긴다고 못박았다. AC-45 의 열두 필드 판정이 그대로 성립하며, § Architecture 의 '이관된 게이트 입력이 모두 열둘 안에 있다' 는 주장과도 어긋나지 않는다 | 치환 없음 |
| R8.3 | SPEC-19 | AC-67(일치), AC-89(일치) | 세 번째 조건(미확인 근거는 사유와 함께)에 AC-89 를 새로 배정해 추적표 서두와 D12 가 선언한 '조건마다 독립 AC' 불변식을 회복했다. AC-67 의 판정 범위는 줄지 않았다 | 치환 없음 |
| R9.2 | SPEC-20 | AC-73(일치) | 두 절의 계층과 위치를 고정해 C1 과 공식 게이트 `required_sections` 의 판정 기준이 구현자 선택에 좌우되지 않게 했다. 이 Spec 자신의 절 배치와 같은 규칙이라 산출물과 템플릿이 어긋나지 않는다 | 치환 없음 |
| R9.8 | SPEC-23 | AC-85(일치), AC-86(일치), AC-87(일치), AC-90(일치) | 가드에 임포트 가능한 `is_supported` 를 두어 CMD-7 을 못 도는 환경에서도 거부 분기가 판정된다. AC-86 의 '인라인 복제 금지' 전제를 깨지 않는다 — 함수는 여전히 파일 하나에만 있다 | 치환 없음 |
| R1.4 | SPEC-21 | AC-5(일치), AC-6(일치), AC-7(일치), AC-80(일치) | AC-5 에 검사 대상 파일 범위·문단 경계·금지 토큰 다섯을 명시하고, 그 규범의 근거를 R1.4 본문에 새로 넣었다 — 플래그 문자열이 나타나는 모든 위치가 금지 문언과 같은 문단 안이어야 하고 실행 가능한 펜스 코드 블록에는 나타나지 않아야 한다는 문장이다(스냅숏 `snapshots/spec-r1.md:94` 에는 없던 문장이다). AC-6·AC-7 의 판정 대상은 그대로이고, 검사 대상에 `docs/quality-goal-maintenance.md` 를 넣은 것이 R9.3 이 그 문서에 플래그 점검 항목을 추가하도록 요구하는 것과 일치한다. `SKILL.md` § Safety rules 는 건드리지 않으므로 AC-80(CMD-4)의 보존 전제도 유지된다 | 치환 없음 |

요구사항 정의를 건드리지 않고 서술만 고친 세 건은 위 표에 행이 없다.

- **SPEC-18** — § Failure behavior 표에 이 작업이 만드는 거부 계약 넷(`--formal-round` 불일치, `--invocation-status` 열거 위반, `failed` 인데 `--stderr-path` 부재, `light` 모드 호출)의 행을 추가했다. D11 이 strict-only 소절을 제거하며 내세운 "복구 경로는 § Failure behavior 표가 전부 다룬다" 는 근거가 이제 실제 커버리지와 일치한다.
- **SPEC-22** — § Architecture 의 "다섯이 이관된 게이트가 읽어야 할 값 전부다" 를 일곱(`checklist`·`attempt` 포함)으로 고쳤다. Non-goal 12 의 분리 전제를 뒷받침하는 문장이 사실과 맞게 됐다.
- **SPEC-24** — § Interfaces 의 실행 순서 그림에 author 호출 직후의 무변경 digest 비교(R1.9)와 `git status --porcelain` 쓰기 범위 확인(R1.6)을 넣었다.

라운드 1 리뷰가 재현하지 못한 결정적 검증(테스트 스위트, SKILL.md 행 수, 인터프리터별 종료 코드, `.claude/quality-state/` 무시)은 원 출력을 `.claude/quality-state/<task-id>/evidence/deterministic-baseline.md` 에 남겨 라운드 2 근거로 첨부했다. codex-cli 의 `--yolo` 수용 여부만은 그 플래그를 붙인 호출이 `SKILL.md` § Safety rules 로 금지돼 재현하지 않았고, 그 사실을 같은 파일에 적었다.
