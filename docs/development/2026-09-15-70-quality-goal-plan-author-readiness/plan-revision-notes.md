## 라운드 2 개정

공식 Plan 리뷰 라운드 1(74점, REVISE, blocker `PLAN-01`·`PLAN-02`)의 여덟 finding 을 모두 해소했다. 가장 큰 변경은 태스크 구조를 T1~T10 에서 T1~T8 로 재편해 **모든 태스크가 자기 테스트를 자기 안에서 작성**하게 한 것(PLAN-02)과, `### Plan` 이 `### Plan author` 의 문자열 접두라는 사실을 T6 의 명시적 토큰 금지 제약으로 다룬 것(PLAN-01)이다. 그 결과 Spec 요구사항 전체의 태스크 매핑이 바뀌었으므로 아래 표는 35개 요구사항을 모두 싣는다.

| 요구사항 | 해소 finding | 함께 바뀐 항목 | 상호작용 판정 | 치환 근거 |
|---|---|---|---|---|
| R1.1 | PLAN-01 | AC-1 (일치 — 해당 finding 해소가 이 AC 의 판정을 더 정확하게 만든다) | T6 의 접두 토큰 금지는 새 절을 요약으로 묶는 제약일 뿐 작성 주체 계약의 의미를 바꾸지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R1.2 | PLAN-02 | AC-2 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R1.3 | PLAN-02 | AC-3 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R1.4 | PLAN-02 | AC-4 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R1.5 | PLAN-02 | AC-5 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R1.6 | PLAN-07 | AC-6 (일치 — 해당 finding 해소가 이 AC 의 판정을 더 정확하게 만든다) | AC-6 의 기록 거부 절을 CMD-6 회귀로 귀속시켜도 CMD-4 의 byte 불변 판정은 그대로여서 AC-34 와 충돌하지 않는다. | 자체 개정 |
| R2.1 | PLAN-02 | AC-7 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R2.2 | PLAN-02 | AC-8 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R2.3 | PLAN-02 | AC-9 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R2.4 | PLAN-02 | AC-10 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R2.5 | PLAN-02 | AC-11 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R2.6 | PLAN-02 | AC-12 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R3.1 | PLAN-02 | AC-13 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R3.2 | PLAN-02 | AC-14 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R3.3 | PLAN-02 | AC-15 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R3.4 | PLAN-02 | AC-16 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R3.5 | PLAN-02 | AC-17 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R3.6 | PLAN-02 | AC-18 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R4.1 | PLAN-02 | AC-19 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R4.2 | PLAN-06 | AC-20 (일치 — 해당 finding 해소가 이 AC 의 판정을 더 정확하게 만든다) | checklist fixture 를 T4 에서 만들고 CMD-2 로 판정하면 T2 의 validator fixture 집합이 커지지만 미러 동기화 규칙이 신규 파일 추가를 이미 포함해 공백이 생기지 않는다. | 자체 개정 |
| R4.3 | PLAN-06 | AC-21 (일치 — 해당 finding 해소가 이 AC 의 판정을 더 정확하게 만든다) | supplied target 판정을 CMD-1 로, payload 내부 규칙을 CMD-2 로 나눈 분리는 R7.2 의 이름 규약을 그대로 쓰므로 새 모순이 없다. | 자체 개정 |
| R5.1 | PLAN-02 | AC-22 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R5.2 | PLAN-02 | AC-23 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R5.3 | PLAN-02 | AC-24 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R5.4 | PLAN-02 | AC-25 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R5.5 | PLAN-02 | AC-26 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R6.1 | PLAN-02 | AC-27 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R6.2 | PLAN-02 | AC-28 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R6.3 | PLAN-02 | AC-29 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R6.4 | PLAN-02 | AC-30 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R7.1 | PLAN-03 | AC-31 (일치 — 해당 finding 해소가 이 AC 의 판정을 더 정확하게 만든다) | 보존 helper 를 T7 로 두고 CMD-10 을 T8 에서 재실행하면 T6 시점 증거가 낡는 문제가 닫히며 QG_BASE 이동 결정과도 어긋나지 않는다. | 자체 개정 |
| R7.2 | PLAN-02 | AC-32 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R7.3 | PLAN-02 | AC-32 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R7.4 | PLAN-02 | AC-33 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립), AC-34 (일치 — 태스크 재배정 후에도 판정 명령과 기대 결과가 그대로 성립) | 모든 태스크가 자기 테스트를 자기 안에서 작성하도록 바꾼 재구조화이며 이 요구사항의 AC 판정 명령은 변하지 않아 다른 해소의 전제를 깨뜨리지 않는다. | 자체 개정 |
| R7.5 | PLAN-01 | AC-1 (일치 — 해당 finding 해소가 이 AC 의 판정을 더 정확하게 만든다), AC-35 (일치 — 해당 finding 해소가 이 AC 의 판정을 더 정확하게 만든다) | 새 절을 요약으로 제한하고 정본을 readiness-policy 로 두는 선택은 SPEC-06 의 10행 예산과 같은 방향이라 두 제약이 서로를 강화한다. | 자체 개정 |
