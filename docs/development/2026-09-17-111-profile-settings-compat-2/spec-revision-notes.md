## 라운드 2 개정

| 요구사항 | 해소 finding | 함께 바뀐 항목 | 상호작용 판정 | 치환 근거 |
|---|---|---|---|---|
| R1.1 | SPEC-003 | AC-1(일치: 기존 official-status exact argv와 empty FD를 고정), AC-2(일치: enrollment도 같은 exact argv를 유지) | official-status 기대 argv 변경은 enrollment argv 계약이나 prescan 경계 결정을 바꾸지 않고 두 auth 경로를 같은 하드닝 계약으로 수렴시킨다. | 기존 bare auth argv 단언을 R1.1의 하드닝 argv로 치환 |
| R1.2 | SPEC-004, SPEC-005 | AC-3(일치: 최종 role argv의 exact-one 계약 유지), AC-17(일치: helper version·provider별 help·installed-checker를 고정) | provider별 help 분기는 codex 광고를 불변으로 두면서 Claude five-option 검사를 충족하므로 role argv 검증 전제에 새 공백을 만들지 않는다. | 범위 내 임의 version과 공유 help라는 불완전 결정을 exact version·provider별 광고로 치환 |
| R1.3 | SPEC-001 | AC-4(일치: 미하드닝 경로는 계속 fail-closed), AC-16(일치: selected account-home 성공과 주입 부재를 동시 판정), AC-20(일치: 네 prescan 지점과 기존 테스트의 새 기대값을 판정) | project prescan 유지와 selected-home 조건부 우회·최종 argv 재검증을 분리해 compatibility 해소가 project 보안 차단을 약화하지 않는다. | 호출 지점별 상태가 없던 일반 원칙을 네 지점의 유지·조건부 skip·제거 결정으로 치환 |
| R2.1 | SPEC-001 | AC-6(일치: 16-key status 성공 유지), AC-16(일치: fixture를 selected account-home으로 한정해 project 차단과 구분) | status 성공 fixture의 위치를 명시해 AC-20의 project-settings 차단과 모순되지 않으며 provider-default 호환성 목표를 그대로 보존한다. | 모호한 동일 settings 위치를 selected account-home으로 치환 |
| R2.2 | SPEC-001 | AC-7(일치: strict role provider 1회 성공 유지), AC-16(일치: 하드닝 argv와 주입 0회를 함께 판정) | strict role 성공은 selected-home 우회 조건을 모두 통과한 경우로 한정되어 forged admitted argv 거부 전제를 깨지 않는다. | 일반 role 성공 서술을 검증된 selected-home admitted 경계로 구체화 |
| R2.3 | SPEC-002 | AC-8(일치: 실행 가능한 lifecycle 명령을 두 status에 동일 적용), AC-9(일치: 운영 문서와 placeholder 치환 규칙을 동일 고정) | status 필수 인자를 추가하되 helper에는 실제 parser가 받는 세 옵션만 남겨 lifecycle 단계 간 CLI 계약 혼입을 방지한다. | 실행 불가능한 status 예시를 registry·verifier가 포함된 exact 명령으로 치환 |
| R3.1 | SPEC-001 | AC-10(일치: auth 경로 sentinel·MCP·env 0회 유지), AC-11(일치: role 경로 주입 0회 유지), AC-16(일치: compatibility와 security를 동일 fixture에서 결합) | account-home 성공 기대를 sentinel 대체 단언과 결합해 prescan 우회가 보안 테스트 약화로 해석될 여지를 없앤다. | 성공 여부만 있던 account-home 기대를 하드닝 argv·부작용 0회 결합 판정으로 치환 |
| R3.2 | SPEC-001, SPEC-003, SPEC-004, SPEC-005 | AC-1(일치: official-status argv 재정의), AC-12(일치: 추가 회귀의 격리 원칙 유지), AC-13(일치: 29개 기대값 전부 불변), AC-14(일치: 세 재정의 목록과 나머지 기대값 불변), AC-17(일치: exact version·provider별 help·checker exit 0), AC-19(일치: fixture bytes 불변), AC-20(일치: prescan 기존 테스트의 부분 재정의) | 세 기존 테스트 재정의를 한 파일 안에 한정하고 installed-checker·전체 suite·fixture digest를 함께 요구하므로 다른 finding 해소나 방향 B scope에 충돌하지 않는다. | 기존 테스트 전부 불변이라는 모순된 서술을 정확한 세 테스트 재정의 목록과 나머지 불변 계약으로 치환 |
| R3.3 | SPEC-002 | AC-9(일치: 두 문서의 exact lifecycle block), AC-15(일치: 재정의는 기존 허용 파일 안에 한정), AC-18(일치: 설정 격리 문구와 lifecycle block 공존), AC-19(일치: 문서 변경이 fixture 수정 권한을 만들지 않음) | 실행 가능한 문서 명령과 치환 규칙을 추가해도 10-file scope, 직전 산출물 보존, fixture byte 불변은 모두 유지된다. | 필수 registry·verifier가 빠진 문서 계약을 R2.3과 동일한 명령으로 치환 |
