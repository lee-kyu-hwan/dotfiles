# 독립 검증 — 공식 Code 리뷰 라운드 3 제출본

- 대상: 공식 리뷰 r2 의 `CODE-27`~`31`(r9) 과 6차 diff 점검의 `CODE-R6-01`(r10)
- 워크스페이스 지문: `record-verification` 으로 state.json 에 기록
- 판정 명령 전사: `command-transcript-r3.txt` — 열두 명령의 문자열과 종료 코드,
  실행 전후 구현 트리 다이제스트, `git status --porcelain`, `__pycache__` 잔재 수

## 요약

| 항목 | 결과 |
|---|---|
| 판정 명령 CMD-1~12 | **12/12 종료 코드 0** |
| 전체 스위트 | **83 tests OK** |
| 변이 자기점검 (누적) | **39/39 잡힘, 놓침 0** |
| `__pycache__` 잔재 | 0 |
| 구현 트리 다이제스트 실행 전후 | 동일 — 판정 명령이 대상을 바꾸지 않았다 |
| 무관 변경 | 없음 (Codex 가 만든 `AGENTS.md` 1건은 r6 시점에 회수) |

## r9·r10 변이 자기점검

| 변이 | 대상 | 결과 |
|---|---|---|
| Markdown 0건 파싱을 유효 리뷰로 되돌림 | CODE-28 / R8.1 | 잡힘 |
| 보고서를 익명 view 에서 렌더링으로 되돌림 | CODE-27 / R7.2 | 잡힘 |
| Synthesis·갈린 지점 출처 복원 제거 | CODE-27 / R7.2 | 잡힘 |
| 마스크 복원(`restore()`) 제거 | CODE-27 / D2 | 잡힘 |
| synthesis 에 `restore()` 미적용 | CODE-27 / D2 | 잡힘 |
| 종합자 입력 마스킹 제거 (역방향) | R6.2 | 잡힘 |
| 완전 group map 전달 제거 | CODE-R6-01 / R7.2 | 잡힘 |
| 그룹 문자 fallback 복원 | CODE-R6-01 / R7.2 | 잡힘 |
| 인용된 `A=`/`B=` relabel 제거 | CODE-R6-01 / R7.2 | 잡힘 |
| claims 값의 relabel 제거 | CODE-R6-01 / R7.2 | 잡힘 |
| group map 포함 검사 제거 | CODE-R6-01 | 잡힘 |

## 변이 점검이 두 번 놓쳤고 두 번 다 픽스처가 원인이었다

| 시점 | 놓친 경계 | 원인 |
|---|---|---|
| r5 | 마스크 정렬 키 `(-len, token)` → `len` | 픽스처에 동길이 토큰이 없어 결함이 드러나지 않았다 |
| r9 | 마스크 복원(`restore()`) 제거 | 픽스처의 종합자가 마스킹 토큰을 되돌려주지 않았다 |

**둘 다 테스트가 옳은 성질을 검사하고 있었는데 픽스처가 약했다.** 스위트 통과·커버리지 어느
신호로도 보이지 않으며, 변이 점검만이 드러냈다. 그리고 변이 점검조차 픽스처를 고쳐야
드러났으므로, **변이 점검을 했다는 사실이 변이 점검의 완전성을 뜻하지 않는다.**

`CODE-R4-05`(4차 점검)도 같은 계열이다 — 오케스트레이터의 15개 변이 목록 **밖**의 다섯 경계에서
나왔다.

## 오케스트레이터가 반박한 finding 1건

`CODE-R4-01`(4차 점검, High) — 익명 view 가 실제 `source`("claude"/"codex")와 모델명 alias 를
대소문자 무시로 마스킹해야 한다는 요구.

반박 근거: (1) `AC-37`(spec.md:136)이 문자열 불변식을 다섯 생산자 에이전트명과 원문 ID 로
**열거 한정** 한다. (2) 대상 저장소의 경로가 `dot_claude/skills/dual-review/…` 이므로 "claude"
를 대소문자 무시로 치환하면 모든 finding 의 `file` 값이 파괴돼 종합자가 파일을 식별할 수 없다.

**공식 리뷰 라운드 2 가 이 기각을 지지했다**(`code-review-r2.json` evidence 14). 잔여 실질
1건 — `codex` 는 오늘 실제로 마스킹되는데 `claude` 는 아닌 비대칭 — 은 `CODE-29` 로 기록돼
r9 에서 대칭화·문서화됐다.
