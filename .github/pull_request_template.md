<!-- PR 제목은 Conventional Commits 형식으로 작성한다 -->
<!-- 예: feat(zsh): claude-profile1 진입점을 추가한다 (#131) / fix(quality-goal): ... -->
<!-- 제목 끝에 관련 이슈 번호를 (#N) 으로 붙이는 것이 이 저장소 관례다 -->

## 개요 (필수)

<!-- 이 PR이 무엇을 왜 바꾸는지 설명한다. 문제 → 해결 순서로 쓰면 읽기 쉽다 -->

> ⚠️ **관련 이슈를 반드시 연결한다.** 키워드가 없으면 머지해도 이슈가 자동으로 닫히지 않는다.
> 실제로 #105·#146·#149 가 PR 제목에만 번호를 적어 수동으로 닫아야 했다.

**관련 이슈**:

<!--
  Closes #N   이 PR로 이슈가 완결될 때
  Refs #N     관련은 있지만 완결은 아닐 때 (spike, 에픽, 후속 작업이 남은 경우)

  예) Closes #146
      Refs #122
-->

## 주요 변경 사항

-
-

## 테스트 (필수)

<!-- 해당하는 것만 남기고 실제로 실행한 결과를 적는다. "돌렸다"가 아니라 출력을 적는다 -->

- [ ] `python3 -m unittest tests.<모듈>` — 결과:
- [ ] `chezmoi execute-template -S . < <파일>.tmpl` 렌더링 성공
- [ ] `zsh -n` / `bash -n` 문법 검사 통과
- [ ] `chezmoi diff` 로 의도한 변경만 나오는지 확인 (되돌아가면 안 되는 drift가 없는지)
- [ ] 실제 동작 확인 — 어떻게:

**검증 환경**: <!-- machine_type (work/personal/server), 관련 도구 버전 등 -->

## 체크리스트

- [ ] 코드 자체 리뷰 완료
- [ ] 스킬·설정 문서를 고쳤다면 **두 복사본을 모두 반영**했다
      (`dot_agents/skills/<name>/` 와 `dot_claude/skills/<name>/` — frontmatter 차이는 유지)
- [ ] 런타임 도구가 고쳐 쓰는 파일을 건드렸다면 #147 의 대응 패턴(합치기/ignore/최신화) 중
      무엇을 왜 골랐는지 본문에 적었다
- [ ] 워크트리를 만들었다면 Orca 계보에 연결했다 (`orca worktree set --parent-worktree`)
- [ ] 관련 문서 업데이트 (필요시)
- [ ] 되돌리는 방법이 명확하다

---

## 추가 정보 (선택)

### 대안

<!-- 고려했지만 채택하지 않은 방법과 그 이유. 나중에 같은 판단을 재현할 수 있게 한다 -->

### 알려진 제약

<!-- 이 PR이 해결하지 않는 것, 남는 문제 -->

### 참고 자료

<!-- 관련 이슈·커밋·외부 문서 -->
